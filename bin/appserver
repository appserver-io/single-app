#!/bin/bash

# Set the default appserver installation directory
APPSERVER_ROOT="/opt/appserver"

# Start the appserver itself
$APPSERVER_ROOT/bin/php -dappserver.php_sapi=appserver server.php -c $APPSERVER_ROOT/etc/appserver/appserver-single-app.xml